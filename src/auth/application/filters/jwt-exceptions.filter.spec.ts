import { JwtExceptionsFilter } from './jwt-exceptions.filter';

describe('JwtExceptionsFilter', () => {
  it('should be defined', () => {
    expect(new JwtExceptionsFilter()).toBeDefined();
  });
});
